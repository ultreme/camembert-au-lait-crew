version: '2'

services:
  calc-www:
    labels:
      io.rancher.container.pull_image: always
    image: ultreme/calcbiz:latest
    environment:
      - VIRTUAL_HOST=calc-api.m.42.am
      #- VIRTUAL_PORT=
    networks:
      - default
      - service-proxy

networks:
  service-proxy:
    external: true
